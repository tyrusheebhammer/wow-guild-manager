<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorAccent</item>

    </style>

    <style name="DialogEditText">
        <item name="android:textColor">@color/colorTextDescription</item>
        <item name="android:textColorHint">@color/colorTextDescription</item>
        <item name="android:backgroundTint">@color/colorTextDescription</item>
        <item name="android:buttonTint">@color/colorTextDescription</item>
        <item name="android:colorControlActivated">@color/colorAccent</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="AlertDialogCustom" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textColorAlertDialogListItem">@color/colorTextDescription</item>
        <item name="android:windowBackground">@drawable/alert_window_background</item>
    </style>

    <style name="AlertButtonCustom" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/colorAccent</item>
        <item name="rippleColor">@color/colorAccent</item>
        <item name="android:strokeWidth">1</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>

